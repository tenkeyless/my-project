import time
import random

class DijkstraAlgorithm:
    def run(self, graph, start, goal):
        # 실제 구현이 들어갈 자리
        time.sleep(random.uniform(0.01, 0.1))  # mock 시간 지연
        explored_nodes = random.randint(100, 200)  # 임의로 탐색된 노드 수
        path = [start, goal]  # 임의로 경로 설정
        return path, explored_nodes